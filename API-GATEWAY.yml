eureka:
    client:
        fetch-registry: true
        register-with-eureka: true
        service-url:
            defaultZone: http://eureka-server:8761/eureka
server:
    port: 9000
spring:
    application:
        name: API-GATEWAY
